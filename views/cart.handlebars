{{!-- filepath: /home/luka-lattanzi/Documentos/programming/CoderHouse-ProyectoBackend-I/src/views/cart.handlebars --}}
<div class="container my-4">
  <h1>Carrito</h1>
  {{#if cart.products.length}}
  <ul class="list-group">
    {{#each cart.products}}
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <div>
        <h5>{{this.product.title}}</h5>
        <p>{{this.product.description}}</p>
        <p><strong>Precio:</strong> ${{this.product.price}}</p>
        <p><strong>Cantidad:</strong> {{this.quantity}}</p>
      </div>
      <img 
        src="{{#if this.product.thumbnail}}{{this.product.thumbnail}}{{else}}/img/no-image.jpg{{/if}}" 
        alt="{{this.product.title}}" 
        style="width: 100px; height: auto;" 
      />
    </li>
    {{/each}}
  </ul>
  {{else}}
  <p>El carrito está vacío.</p>
  {{/if}}
</div>